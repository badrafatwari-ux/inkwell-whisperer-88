import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.writerideabooster.app",
  appName: "Writer Idea Booster",
  webDir: "dist/client",
};

export default config;
